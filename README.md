# rvcas Homebrew tap

This repository contains formulas for installing rvcas's projects using [Homebrew](https://brew.sh) on Mac.

## Installing

To fetch the tap, just run `brew tap rvcas/homebrew-tap`. You can also just install any rvcas project by name:

```sh
brew install rvcas/tap/mach
```
